<h1><%= @movie.title %></h1>

<%= image_tag @movie.poster rescue nil %>
<p><%= @movie.plot %></p>
<p>Release year: <%= @movie.year %></p>

<%= link_to "Edit", edit_movie_path(@movie) %>
<%= link_to "Delete", movie_path(@movie),
		method: :delete,
		data: { confirm: "Are you sure?" } %>

